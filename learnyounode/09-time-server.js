'use strict';

let fs = require('fs')
let http = require('http')
let bl = require('bl')
let net = require('net')


var server = net.createServer(function (socket) {
  // socket handling logic
})
server.listen(8000)
