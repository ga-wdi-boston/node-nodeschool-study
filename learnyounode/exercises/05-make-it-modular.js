'use strict'

let reading = require('./05-reading-function');
let filtering = require('./05-reading-function');
let fs = require('fs');

fs.readdir(dir, function(err, list) {
  let files = list.toString().split(',');
}
